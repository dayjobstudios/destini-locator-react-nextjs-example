'use client'

import { Locator } from '@destini/react-locator'

/**
 * The following import is included in the docs for @destini/react-locator
 * but is not actually present in the package, so it's commented out here...
 *
 * import '@destini/react-locator/dist/index.css'
 */

export const DestiniLocator = () => {
  return <Locator clientId="emotionalutilitybeverage" />
}
